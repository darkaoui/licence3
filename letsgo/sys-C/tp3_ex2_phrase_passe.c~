#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string>

char* phrase_passe(int nombre, int taille, char* tab[taille]){
  int longueur =0;
  int choisies[nombre];

  if(taille == 1){
    fprintf(stderr,"Erreur,aucun mot rentre!\n");
    exit(1);
  }

  else if(nombre>taille){
    fprinft(stderr,"Erreur,le nombre de mots a choisir ne peut exceder la taille du tableau!\n");
    exit(2);
  }

  
    
}
